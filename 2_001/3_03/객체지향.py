#객체를 이용한 프로그램으로 객체는 속성과 기능으로 구성된다.
# 객체 = 속성(attribute) + 기능(function)

# 객체는 클래스에서 생성된다.
# 예를들어, 자동차 클래스 = 속성(색상, 길이, 가격..) + 기능(전진,후진,정지...)
# 이 자동차 클래스에서 여러개의 객체가 생성된다.

# 즉, 클래스는 객체를 만들기 위한 틀이다. 마치 붕어빵 틀 처럼

# 우리가 할 것은 클래스를 만드는 것!

# 객체 사용의 장점 : 코드 재사용,모듈화에 좋다